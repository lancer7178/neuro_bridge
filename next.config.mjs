/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "images.pexels.com"] // السماح بجلب الصور من Unsplash و Pexels
  },
};

export default nextConfig;
